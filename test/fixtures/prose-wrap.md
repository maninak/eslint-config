# Prose wrap fixture

This paragraph is hand-wrapped at roughly seventy columns so that a change
to `proseWrap` is observable: under `preserve` these newlines survive, under
`never` the whole paragraph collapses onto one line.

- A list item that is also hand-wrapped across two source lines, which is the
  other shape the setting governs.
