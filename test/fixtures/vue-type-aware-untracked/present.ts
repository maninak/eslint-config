// Present only so the tsconfig has at least one input and TypeScript does not reject it
// outright, isolating the failure mode where the SFC alone is outside the project.
export const present = true
